import { ResizeInputDirective } from './resize-input.directive';

describe('ResizeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeInputDirective();
    expect(directive).toBeTruthy();
  });
});
