/**
 * Test setup for zoneless Angular tests.
 *
 * Zone.js is no longer used — Angular runs with provideZonelessChangeDetection().
 * TestBed automatically uses zoneless change detection when zone.js is absent.
 */
